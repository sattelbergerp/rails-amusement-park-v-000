<h2><%= @attraction.name %></h2>
<%= form_tag ride_attraction_path(@attraction) do%>
    <%= submit_tag "Go on this ride" %>
<% end %>
<% if current_user && current_user.admin %>
    Admin:
    <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<%end%>
